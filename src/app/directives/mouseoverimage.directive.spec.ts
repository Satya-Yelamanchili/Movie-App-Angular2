import { MouseoverimageDirective } from './mouseoverimage.directive';

describe('MouseoverimageDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseoverimageDirective();
    expect(directive).toBeTruthy();
  });
});
